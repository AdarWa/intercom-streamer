services:
  intercom-streamer:
    build: .
    container_name: intercom_streamer
    restart: unless-stopped
    ports:
      - "8000:8000"  # Expose MJPEG stream
    devices:
      - "/dev/video0:/dev/video0"  # USB camera